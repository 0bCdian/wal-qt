{
  "name": "wal-qt-renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint . --config .oxlintrc.json --tsconfig tsconfig.json",
    "lint:advisory": "npm run lint || true",
    "format": "oxfmt --config .oxfmtrc.json --write .",
    "format:check": "oxfmt --config .oxfmtrc.json --check .",
    "format:check:advisory": "npm run format:check || true",
    "check:all": "npm run lint:advisory && npm run format:check:advisory && npm run typecheck && npm run test",
    "check:all:strict": "npm run lint && npm run format:check && npm run typecheck && npm run test",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "dependencies": {
    "gsap": "^3.14.2"
  },
  "devDependencies": {
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0",
    "typescript": "~5.6.2",
    "vite": "^6.4.2",
    "vitest": "^4.1.4"
  }
}
